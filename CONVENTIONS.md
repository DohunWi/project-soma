# 프로젝트 컨벤션

깃허브가 익숙하지 않아도 따라올 수 있게 최소한만 정합니다.
**규칙 하나가 지켜지지 않으면 그 규칙을 고칩니다. 사람을 탓하지 않습니다.**

---

## 1. 브랜치

```
main                    항상 동작하는 상태. 직접 푸시 금지
└─ feat/<폴더>/<내용>   기능 추가
└─ fix/<폴더>/<내용>    버그 수정
└─ docs/<내용>          문서만
```

`<폴더>` 는 본인 담당 최상위 디렉터리입니다.

```
feat/vision/blink-ear
fix/chair/serial-port-autodetect
docs/contracts-v1
```

**브랜치는 짧게 삽니다.** 사흘 넘게 안 합치면 충돌이 커집니다.
길어질 것 같으면 쪼개서 먼저 합치세요.

## 2. 커밋

```
<타입>(<폴더>): <한 줄 요약>

<왜 이렇게 했는지. 무엇을 했는지가 아니라>
```

타입: `feat` `fix` `docs` `refactor` `test` `chore`

```
fix(chair): 시리얼 포트 하드코딩 제거

'COM3' 이 박혀 있어 macOS 에서 열리지 않았다.
.env → 자동탐색 → --port 순으로 결정하도록 바꿨다.
```

- **한 줄 요약은 50자 이내, 마침표 없이.**
- 본문은 "무엇을" 보다 **"왜"** 를 씁니다. 무엇을 했는지는 diff 에 있습니다.
- 커밋 하나에 한 가지만. 리팩터링과 기능 추가를 섞지 않습니다.
- **`.env`, 캘리브레이션 파일, 로그, `__pycache__` 는 커밋하지 않습니다.**

## 3. PR

| 대상 | 리뷰어 |
|---|---|
| 본인 폴더 | 아무나 1명 |
| `docs/contracts/` | **전원** |

PR 본문에 세 줄만 씁니다.

```
무엇:   한 줄
왜:     한 줄
확인법: 리뷰어가 어떻게 확인하는지 (명령어 한 줄이면 최고)
```

- **남의 폴더를 고쳐야 하면 PR 전에 그 담당자에게 말합니다.**
- 리뷰는 하루 안에. 못 하면 "오늘 못 봄" 이라고 말해주세요. 침묵이 제일 나쁩니다.
- 승인 후 **Squash merge**. 커밋 히스토리가 깔끔해집니다.

## 4. 계약이 먼저

**`docs/contracts/` 를 고치지 않고 필드를 추가하지 않습니다.**

순서:
1. 계약 파일 수정 → PR (전원 리뷰)
2. 합쳐진 뒤 각자 코드 수정

이 순서를 어기면 이전 레포에서 있었던 일이 반복됩니다 —
프론트가 존재하지 않는 필드를 기대하는 29KB 짜리 UI 를 만들었고,
백엔드와 포트조차 달랐습니다.

받는 쪽은 **모르는 필드를 무시**합니다. 그래야 한쪽만 먼저 배포해도 안 깨집니다.

## 5. 코드

### Python
- **PEP 8**, 들여쓰기 4칸, 한 줄 100자까지
- 파일·함수·변수는 `snake_case`, 클래스는 `PascalCase`, 상수는 `UPPER_SNAKE`
- **모듈 맨 위에 docstring** — 이 파일이 무엇이고 어떻게 쓰는지
- 임계값·포트·경로 같은 숫자는 **모듈 상단 상수** 또는 `.env`. 코드 중간에 박지 않습니다
- 주석은 **왜** 를 씁니다. 코드가 이미 무엇인지 말하고 있습니다

```python
# 나쁨:  pressure 합을 구한다
# 좋음:  방석 두께 때문에 정수리 압력이 낮게 나와 임계를 100 으로 낮췄다
```

### 예외를 삼키지 않습니다

```python
# 나쁨 — 이전 app.py 가 이렇게 돼 있었습니다
except Exception as e:
    print(f"에러: {e}")

# 좋음
except (KeyError, ValueError) as e:
    log.warning("payload 형식 오류, 이 샘플은 버립니다: %s", e)
```

무엇이 실패했는지 모르면 시연 중에 원인을 못 찾습니다.

### JavaScript
- `camelCase`, 세미콜론 사용, `const` 우선 `let` 차선, `var` 금지
- **판정 로직을 프론트에 넣지 않습니다.** 점수·상태는 서버에서 옵니다

### Arduino
- 핀 번호는 `const int PIN_*` 로 파일 상단에
- **`delay()` 로 루프를 막지 않습니다.** `millis()` 비교를 쓰세요.
  진동 중에 센서 읽기가 멈추면 안 됩니다

## 6. 네이밍

| 대상 | 규칙 | 예 |
|---|---|---|
| 계약 필드 | `snake_case` | `blink_rate`, `face_distance_cm` |
| 단위가 있는 값 | **이름에 단위를 붙입니다** | `_cm`, `_sec`, `_ms` |
| 시각 | `t` (Unix epoch 초) | `"t": 1725345600.123` |
| 지속 시간 | `*_sec` | `static_hold_sec` |
| 상태 문자열 | `UPPER_SNAKE` | `NORMAL` `CAUTION` `RISK` `ABSENT` |

**단위를 이름에 넣는 규칙이 실제로 사고를 막습니다.** 거리센서는 mm 를 내고
화면에는 cm 로 표시합니다. `distance` 만 있으면 어느 쪽인지 알 수 없습니다.

## 7. 하드코딩 금지 목록

| 절대 코드에 넣지 않는 것 | 어디에 |
|---|---|
| DB 접속 정보 | `.env` |
| 시리얼 포트 | `.env` 또는 자동탐색 |
| 서버 주소·포트 | `.env` |
| 인증 토큰 | `.env` |

**공개 저장소입니다.** 자격증명이 한 번 올라가면 커밋 히스토리에서 지워도
이미 크롤링됐다고 보고 값을 교체해야 합니다.

## 8. 테스트

전부 테스트할 필요는 없습니다. **순수 함수는 반드시 합니다.**

```bash
pytest fusion/          # 상태 판정 — 하드웨어 없이 돌아갑니다
```

`fusion/state.py` 를 고치면 테스트를 같이 고칩니다.
UI·하드웨어는 수동 확인으로 충분합니다.

## 9. 하드웨어 없이 개발합니다

**의자는 1대이고 공용공간에 있습니다.** 기다리지 마세요.

```bash
python tools/mock/stream.py --scenario fatigue    # 가짜 센서
python tools/mock/stream.py --stdout              # 서버조차 없이
python chair/bridge/bridge.py --stdout            # 실제 의자, 서버 없이
```

"하드웨어가 없어서 못 했다" 는 이유가 되지 않도록 하네스를 먼저 만들어 뒀습니다.

## 10. 막혔을 때

30분 넘게 막히면 **팀 채팅에 올립니다.** 다음 세 줄과 함께.

```
하려던 것:
한 것:
나온 것:   (에러 메시지 그대로 붙여넣기)
```

혼자 하루 태우는 것보다 30분 만에 묻는 게 팀 전체에 이득입니다.
